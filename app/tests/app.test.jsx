var expect = require('expect');

// describe - Categorize testing group (visual output)
describe('App', () => {
	// it - Define specific test
	it('Should properly run tests', () => {
		expect(1).toBe(1);
	});
});
